{
  "name": "markdown-resume-editor",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf build",
    "compile": "tsc",
    "copy-static": "cpx \"./*.{html,json}\" build",
    "build": "npm run clean && npm run compile && npm run copy-static",
    "start": "serve build"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "serve": "^14.2.1",
    "typescript": "^5.3.3"
  }
}