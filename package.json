{
  "name": "markdown-resume-editor",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf build",
    "compile": "tsc",
    "copy-static": "cpx \"./*.{html,json}\" build",
    "build": "echo '--- Starting build process for Resume Studio ---' && npm run clean && echo '--- Compiling TypeScript (tsc) ---' && NODE_OPTIONS=--max-old-space-size=4096 npm run compile && echo '--- Copying static assets (cpx) ---' && npm run copy-static && echo '--- Build completed. Contents of build folder: ---' && ls -R build",
    "start": "serve build"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/html2pdf.js": "^0.10.0",
    "@types/marked": "^4.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "cpx": "^1.5.0",
    "dompurify": "^3.0.11",
    "marked": "4.0.10",
    "serve": "^14.2.1",
    "typescript": "^5.3.3"
  }
}